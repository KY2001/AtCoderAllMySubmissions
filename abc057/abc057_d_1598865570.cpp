/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author Kein Yukiyoshi
 */

#include "bits/stdc++.h"
#define int long long
#define rep(i, n)           for (int i = 0; i < (int)(n); i++)
#define REP(i, a, b)        for (int i = (int)(a); i < (int)(b); i++)
#define FOR(i, a)           for (auto &i : a)
#define ALL(obj)            begin(obj), end(obj)
#define MAX(x)              *max_element(ALL(x))
#define MIN(x)              *min_element(ALL(x))
#define SUM(x)              accumulate(ALL(x), 0LL)
#define LOWER_BOUND(A, key) distance(A.begin(), lower_bound(ALL(A), key))
#define UPPER_BOUND(A, key) distance(A.begin(), upper_bound(ALL(A), key))

using namespace std;
const int MOD    = (int)(1e9 + 7);
const int INF    = (int)(1e13 + 7);
const double EPS = 1e-14;
const double PI  = acos(-1);

int CEIL(int a, int b) { return (a >= 0 ? (a + (b - 1)) / b : (a - (b - 1)) / b); } //ceil() for int
int mod(int a, int b) { return a >= 0 ? a % b : a - (b * CEIL(a, b)); }             //always return positive num
int bpm(int a, int b) {                                                             //return x^y in order(log(y))
  int res = 1;
  for (a %= MOD; b; a = a * a % MOD, b >>= 1)
    if (b & 1) res = res * a % MOD;
  return res;
}

int nCk(int a, int b) {
  int ret = 1;
  vector<int> temp{};
  rep(i, b) temp.emplace_back(i + 1);
  rep(j, b) {
    ret *= a - j;
    rep(k, temp.size()) {
      if (ret % temp[k] == 0 and temp[k] > 1) {
        ret /= temp[k];
        temp[k] = 1;
      }
    }
  }
  return ret;
}

class DMaximumAverageSets {
public:
  static void solve(istream &cin, ostream &cout) {
    int N, A, B;
    cin >> N >> A >> B;
    vector<int> v(N);
    rep(i, N) cin >> v[i];
    sort(ALL(v));
    int ans = 0;
    rep(i, A) ans += v[N - 1 - i];
    cout << setprecision(20) << (double)ans / (double)A << fixed << endl;
    int ans2 = 0;
    map<int, int> num;
    FOR(i, v)
    num[i]++;
    if (num[v[N - 1]] >= B) {
      REP(k, A, B + 1) {
        ans2 += nCk(num[v[N - 1]], k);
      }
    } else if (num[v[N - 1]] < B and num[v[N - 1]] >= A) {
      REP(k, A, num[v[N - 1]] + 1) {
        ans2 += nCk(num[v[N - 1]], k);
      }
    } else {
      int temp = 0;
      rep(i, N) {
        if (v[i] > v[N - A]) temp++;
      }
      ans2 = nCk(num[v[N - A]], A - temp);
    }
    cout << max(ans2, 1LL) << endl;
  }
};


signed main() {
  cin.tie(0);
  cout.tie(0);
  ios_base::sync_with_stdio(false);
  cout<<fixed<<setprecision(15);
  DMaximumAverageSets solver;
  std::istream& in(std::cin);
  std::ostream& out(std::cout);
  solver.solve(in, out);
  return 0;
}
