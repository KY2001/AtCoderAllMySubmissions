#include <iostream> //C++の機能を読み込むための命令

using namespace std; // std::を省略して書くことができる

int main() {
  int seconds = 365 * 24 * 60 * 60;

  cout << seconds << endl;
  cout << 2 * seconds << endl;
  cout << 5 * seconds << endl;
  cout << 10 * seconds << endl;
}