#include <bits/stdc++.h>
using namespace  std;

class D {
public:
  static void solve(istream &cin, ostream &cout) {
    cout << "wa" << endl;
  }
};


signed main() {
  D solver;
  std::istream& in(std::cin);
  std::ostream& out(std::cout);
  solver.solve(in, out);
  return 0;
}

